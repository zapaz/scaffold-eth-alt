{
  "name": "@kredeum-template/contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "chain": "anvil",
    "format": "forge fmt",
    "check": "solhint src/**/*.sol",
    "build": "forge compile",
    "test": "forge test",
    "deploy:deploy": "pnpm deploy:all --fork-url $CHAIN --account $ACCOUNT --broadcast --verify",
    "deploy:validate": "pnpm deploy:all --fork-url $CHAIN",
    "deploy:testnet": "ACCOUNT=$TESTNET_ACCOUNT pnpm deploy:deploy",
    "deploy:mainnet": "ACCOUNT=$MAINNET_ACCOUNT pnpm deploy:deploy",
    "deploy:anvil": "pnpm deploy:all --fork-url anvil --account $ANVIL_ACCOUNT --broadcast --password ''",
    "deploy:sepolia": "pnpm deploy:all --fork-url sepolia --account $TESTNET_ACCOUNT --broadcast --verify",
    "deploy:all": "forge script script/DeployAll.s.sol"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "solhint": "^5.0.1"
  }
}