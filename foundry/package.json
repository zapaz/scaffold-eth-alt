{
  "name": "@scaffold-eth-alt/foundry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules out broadcast cache .turbo ",
    "chain": "./anvil.sh",
    "format": "forge fmt",
    "check": "solhint src/**/*.sol",
    "build": "forge compile",
    "test": "forge test",
    "abis": "ts-node mergeAbis.ts",
    "deploy:all": "forge script script/DeployAll.s.sol",
    "deploy:deploy": "pnpm deploy:all --fork-url $CHAIN --account $ACCOUNT --broadcast",
    "deploy:validate": "pnpm deploy:all --fork-url $CHAIN && pnpm run abis",
    "deploy:anvil": "export CHAIN=anvil SENDER=$ANVIL_SENDER ACCOUNT=$ANVIL_ACCOUNT && pnpm deploy:deploy --password '' && pnpm run deploy:validate",
    "deploy:testnet": "SENDER=$TESTNET_SENDER ACCOUNT=$TESTNET_ACCOUNT pnpm deploy:deploy --verify && pnpm run deploy:validate",
    "deploy:mainnet": "SENDER=$MAINNET_SENDER ACCOUNT=$MAINNET_ACCOUNT pnpm deploy:deploy --verify && pnpm run deploy:validate",
    "deploy:sepolia": "CHAIN=sepolia pnpm deploy:testnet",
    "deploy:optimism": "CHAIN=optimism pnpm deploy:mainnet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.7.4",
    "eslint-plugin-json": "^4.0.1",
    "fs-extra": "^11.2.0",
    "prettier": "^3.3.3",
    "solhint": "^5.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}